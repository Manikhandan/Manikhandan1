#include<stdio.h>
int main()
{
int low,high,i,f,t,tmp;
printf("enter the two number");
scanf("%d%d,&low,&high);
if(high<low)
{
temp=low;
low=high;
high=temp;
}
printf("%d%d,low,high);
while(low<high)
{
f=0;
break;
}
}
if(f==0)
printf("%d",low);
++low;
return 0;
}
}
